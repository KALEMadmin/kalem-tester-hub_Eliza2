<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Kalem & Eliza</title>
  <style>
    body {
      margin: 0;
      background-color: #1a1a1a;
      font-family: 'Segoe UI', sans-serif;
      color: #e0e0e0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
canvas {
      background-color: #121212;
      border: 2px solid #b8a9c9;
      border-radius: 12px;
      margin-bottom: 20px;
    }

    textarea {
      width: 100%;
      max-width: 400px;
      background-color: #1a1a1a;
      color: #e0e0e0;
      border: 1px solid #b8a9c9;
      border-radius: 8px;
      padding: 12px;
      font-size: 1em;
    }

    button {
      background-color: #b8a9c9;
      color: #121212;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 1em;
      margin-top: 10px;
    }

  button:hover {
    background-color: #d1c3e0;
    }

    h2 {
      font-weight: 500;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body><h2>Meet Eliza 🪽</h2>
  <canvas id="elizaCanvas" width="300" height="300"></canvas>

  <h2>Report an Issue to Eliza</h2>
  <form id="elizaFeedbackForm">
    <textarea id="feedback" name="feedback" rows="4" required placeholder="Describe the issue..."></textarea><br />
    <button type="submit">Send to Eliza</button>
    <p id="report-status"></p>
  </form>

  <script>
    const canvas = document.getElementById("elizaCanvas");
    const ctx = canvas.getContext("2d");

    ctx.fillStyle = "#1a1a1a";
    ctx.fillRect(0, 0, canvas.width, canvas.height);

    ctx.beginPath();
    ctx.arc(150, 150, 100, 0, 2 * Math.PI);
    ctx.strokeStyle = "#b8a9c9";
    ctx.lineWidth = 4;
    ctx.stroke();

    document.getElementById("elizaFeedbackForm").addEventListener("submit", function(e) {
      e.preventDefault();
      const feedback = document.getElementById("feedback").value;
      document.getElementById("report-status").innerText = "Feedback sent! Eliza is listening.";
    });
  </script>
</body>
</html>